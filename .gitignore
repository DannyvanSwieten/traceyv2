# C++/CMake build directories
build/
build-*/

# Heavy prebuilt deps fetched by scripts/bootstrap_deps.sh (not committed)
deps/oidn/
deps/materialx/
deps/usd/

# Rust target dirs (kept in case examples or external consumers add Cargo)
target/

# Per-image scratch output
*ppm

# macOS metadata
.DS_Store

# Frontend
editor/node_modules/
editor/dist/

# Vite dependency-prebundle cache
editor/.vite/
